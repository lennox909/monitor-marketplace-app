<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFirstApp">

        <activity android:name=".userinterface.AdminDashboardActivity" />
        <activity android:name=".userinterface.SettingsActivity" />
        <activity android:name=".userinterface.ProfileActivity" />
        <activity android:name=".userinterface.OrderConfirmationActivity" />
        <activity android:name=".userinterface.CheckoutActivity" />

        <activity android:name=".userinterface.CartActivity" />
        <activity android:name=".userinterface.ListingDetailActivity" />
        <activity android:name=".userinterface.AddEditListingActivity" />
        <activity android:name=".userinterface.MainActivity" />
        <activity android:name=".userinterface.RegisterActivity" />

        <activity
            android:name=".userinterface.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>