<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFirstApp">

        <!-- Splash is now the launcher -->
        <activity
            android:name=".userinterface.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MyFirstApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".userinterface.LoginActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".userinterface.RegisterActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".userinterface.MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity android:name=".userinterface.AdminDashboardActivity" />

        <activity
            android:name=".userinterface.AddEditListingActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity android:name=".userinterface.ListingDetailActivity" />
        <activity android:name=".userinterface.CartActivity" />

        <activity
            android:name=".userinterface.CheckoutActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity android:name=".userinterface.OrderConfirmationActivity" />

        <activity
            android:name=".userinterface.MyOrdersActivity"
            android:parentActivityName=".userinterface.ProfileActivity" />

        <activity android:name=".userinterface.ProfileActivity" />

        <activity
            android:name=".userinterface.EditProfileActivity"
            android:parentActivityName=".userinterface.ProfileActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".userinterface.SettingsActivity"
            android:parentActivityName=".userinterface.ProfileActivity" />

    </application>
</manifest>